class CartItem {
  constructor(id, quantity, price, title, sum) {
    this.id = id;
    this.quantity = quantity;
    this.price = price;
    this.title = title;
    this.sum = sum;
  }
}

export default CartItem;
